<h1>新規投稿</h1>
<!-- form_with部分 -->
<%= form_with model: @item do |f| %>
  <h4>コーヒー名</h4>
  <%= f.text_field :name %>

  <h4>説明</h4>
  <%= f.text_area :explanation %>

  <h4>購入先</h4>
  <%= f.text_field :shop %>

  <%= f.submit '投稿' %>
<% end %>